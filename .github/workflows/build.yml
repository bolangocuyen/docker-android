name: Build

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build-android:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Build Docker Image
        run: |
          docker build -t bolangocuyen/docker-android .
          docker run --rm --name rn-env bolangocuyen/docker-android /bin/sh -c "npx envinfo"
      - name: Checkout React Native
        run: git clone https://github.com/facebook/react-native.git
      - name: Build React Native
        run: |
          docker run --rm --ulimit nofile=10240 -v "$(pwd)/scripts/":/scripts -v "$(pwd)/react-native/":/react-native -w /react-native bolangocuyen/docker-android /bin/sh -c "/scripts/test-react-native-setup.sh"

  build-fantom:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Build Docker Image
        run: |
          docker build -t bolangocuyen/docker-android .
          docker run --rm --name rn-env bolangocuyen/docker-android /bin/sh -c "npx envinfo"
      - name: Checkout React Native
        run: git clone https://github.com/facebook/react-native.git
      - name: Create reports directory
        run: mkdir -p $(pwd)/fantom-reports
      - name: Run Fantom tests
        continue-on-error: true
        run: |
          docker run --rm --ulimit nofile=10240 -v "$(pwd)/scripts/":/scripts -v "$(pwd)/react-native/":/react-native -v "$(pwd)/fantom-reports/":/react-native/private/react-native-fantom/build/reports -w /react-native react-native-community/react-native /bin/sh -c "/scripts/test-react-native-fantom.sh"
      - name: Upload Fantom build reports
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: fantom-build-reports
          path: fantom-reports/
          retention-days: 30
